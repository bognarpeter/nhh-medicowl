<!-- iterate through every item -->
{{# if diseases }}
    <ul>
        {{# each diseases as |disease key|}}
            <li>{{ disease }}</li>
        {{/each}}
    </ul>
    <ul>
        {{# each symptoms}}
            <li>{{ this.name }}</li>
        {{/each}}
    </ul>
{{/if}}
